#include<bits/stdc++.h>
using namespace std;

//LC-1910 babbar

// string removeOccurrences(string s, string part) {

//         while(s.find(part)!=string::npos)
//         {   
//             //if it comes inside the loop it means the substring is possible in the string
//             s.erase(s.find(part),part.size());
//         }

//         return s;
        

 int main()
{
    
    return 0;
}