#include <bits/stdc++.h>
using namespace std;
/*vector<int> prefixMul;
    ProductOfNumbers() {

    }

    void add(int num) {

    //simply add,no need to take long as it is mentioned in t he question that it wont exceed int size

    if(!prefixMul.empty())
        prefixMul.push_back(prefixMul.back()*num);

    else
       prefixMul.push_back(num);

    if(num==0) //then make the vec empty as the entire prod upto that would be zero
        prefixMul.clear();
    }

    int getProduct(int k) {
        if(k>prefixMul.size()) //means they are considering the part which had been 0
        return 0;

        else if(k==prefixMul.size()) //means the last prod is to be sent
        return prefixMul.back();

        else{ //means valid range

            int toRemove=prefixMul.size()-k-1; //as we have preComputed the values

            return (prefixMul.back()/prefixMul[toRemove]);
        }
    } */
int main()
{

    return 0;
}