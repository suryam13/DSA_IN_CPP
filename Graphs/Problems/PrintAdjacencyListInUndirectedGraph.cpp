#include<bits/stdc++.h>
using namespace std;


//GFG
/*
 // Function to return the adjacency list for each vertex.
    vector<vector<int>> printGraph(int V, vector<pair<int, int>>& edges) {
        
        vector<vector<int>> adj(V);
        
        //undirected graph
        
        for(auto &pairy:edges)
        {
            int u=pairy.first;
            int v=pairy.second;
            
            adj[u].push_back(v);
            adj[v].push_back(u); //since it is an undirected graph ao we have to do the reverse as well
        }
        
        return adj;
    }
*/
 int main()
{
    
    return 0;
}