#include <bits/stdc++.h>
using namespace std;
// SHEET : LC-2095
//    ListNode* deleteMiddle(ListNode* head) {
//          if (head->next == NULL)
//              return NULL;

//         // finding the mid by tortoise and hare algo
//         ListNode* slow = head;
//         ListNode* fast = head;
//         ListNode* prev = NULL;

//         while (fast != NULL && fast->next != NULL) {
//             prev = slow;
//             slow = slow->next;
//             fast = fast->next->next;
//         }

//         // slow would be pointing to the node to be deleted
//         prev->next = slow->next;

//         return head;
//     }
//
int main()
{

    return 0;
}