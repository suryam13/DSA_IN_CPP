#include <bits/stdc++.h>
using namespace std;

// TC - O(n) SC - O(n)
/*

 unordered_set<int> st;
    void makeTree(TreeNode* root, int val) {
        if (!root)
            return;

        if (root->left) {
            st.insert((2 * val) + 1);
            makeTree(root->left, (2 * val) + 1);
        }

        if (root->right) {
            st.insert((2 * val) + 2);
            makeTree(root->right, (2 * val) + 2);
        }
    }
    FindElements(TreeNode* root) {

        int val = 0;
        st.insert(val);
        makeTree(root, val);
    }

    bool find(int target) { return st.count(target); }


*/
int main()
{

    return 0;
}