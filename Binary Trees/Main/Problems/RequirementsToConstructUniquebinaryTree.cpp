#include<bits/stdc++.h>
using namespace std;


/*
If we pre-order and post-order we can make tress but it wont be unique as there would be multiple ways to draw the same tree

To construct unique tree we need inorder tree so as for comaprison
*/
 int main()
{
    
    return 0;
}