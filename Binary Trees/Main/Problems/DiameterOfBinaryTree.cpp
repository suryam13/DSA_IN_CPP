#include<bits/stdc++.h>
using namespace std;
//SHEET : LC-543

/*
 int maxi = 0;

    // just implementing the height function and making changes in this
    int helper(TreeNode* root) {
        if (!root)
            return 0;

        int left = helper(root->left);
        int right = helper(root->right);
        maxi = max(maxi, left + right); //just track the maximum path that is left+right
        
        Just think that i am standing on the root and have the left subtree height as well as the right subtree
        height just add that
        
        return max(left, right) + 1;
    }
    int diameterOfBinaryTree(TreeNode* root) {

        helper(root);
        return maxi;
    }
 */
 int main()
{
    
    return 0;
}