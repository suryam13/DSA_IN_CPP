#include <bits/stdc++.h>
using namespace std;
// SHEET : GFG

/*
    vector<vector<int>> ans; //for final storage

    void helper(Node* root,vector<int> temp)
    {
        if(!root) //if null then please return
        return;

        temp.push_back(root->data); //push current root

        helper(root->left,temp); //left first

        helper(root->right,temp); //then right

        if(!root->left && !root->right)  //if both are NULL then only push as leaf has arrived
        ans.push_back(temp);

        //not popping as i havent passed the temp by reference the pop would be performed automatically at each step
        // temp.pop_back();
    }
    vector<vector<int>> Paths(Node* root) {

        if(!root)
        return ans;

        vector<int> temp;
        helper(root,temp);
        return ans;
    }
*/
int main()
{

    return 0;
}