#include <bits/stdc++.h>
using namespace std;
// SHEET : LC-100
//     bool helper(TreeNode* p, TreeNode* q) {
//          if (!p || !q) // if either of them are null then come to this
//          {
//              if (!p && !q) // means both are null
//                  return true;

//             else
//                 return false;
//         }

//         if (p->val != q->val)
//             return false;

//         bool left = helper(p->left, q->left);
//         if (left == false)
//             return false;

//         bool right = helper(p->right, q->right);
//         if (right == false)
//             return false;

//         return true; //else no worries return true
//     }

//     bool isSameTree(TreeNode* p, TreeNode* q) { return helper(p, q); }
int main()
{

    return 0;
}