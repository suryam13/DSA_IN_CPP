#include <bits/stdc++.h>
using namespace std;

/*
 bool helper(TreeNode* root1, TreeNode* root2) {
        if (!root1 && !root2) // means both are null then return
            return true;

        if ((root1 == NULL && root2 != NULL) ||
            (root1 != NULL && root2 == NULL))   //any of them violates then false
            return false;

        if (root1->val != root2->val)
            return false;

        if (helper(root1->left, root2->right) == false)
            return false;

        if (helper(root1->right, root2->left) == false)
            return false;

        return true;
    }
    bool isSymmetric(TreeNode* root) { return helper(root, root); }
*/
int main()
{

    return 0;
}