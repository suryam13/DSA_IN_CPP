#include <bits/stdc++.h>
using namespace std;

// SHEET : GFG
/*
    bool isBSTTraversal(vector<int>& arr) {

        //in BST all the left elemnts are smaller than the root
        //since it is a inorder traversal so left should be smaller than the root

        for(int i=0;i<arr.size()-1;i++)
        if(arr[i]>=arr[i+1]) return false; //also it should not be same as the values in BST are unique

        return true;
    }

*/
int main()
{

    return 0;
}