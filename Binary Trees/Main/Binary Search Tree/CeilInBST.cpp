#include <bits/stdc++.h>
using namespace std;
// SHEET - GFG 

/*
can also be done using recursion

CODE :
S.C : O(1)
T.C : O(logN) best O(N) worst
// SHEET - GFG 

int findCeil(Node* root, int input) {
  if (root == NULL) return -1;
    
   Node* dummy=root;
   
   int val=-1;
   
   while(dummy)
   {
       if(dummy->data >= input) 
       val=dummy->data;
       
       if(dummy->data >= input) //go left
       dummy=dummy->left;
       
       else
       dummy=dummy->right;
   }
   
   return val;

}

*/
int main()
{

    return 0;
}