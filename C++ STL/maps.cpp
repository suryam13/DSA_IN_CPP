#include <bits/stdc++.h>
using namespace std;

/*
MULTIMAP : Duplicate keys are allowed in multimap.
    multimap<int,int> mapy;
    mapy.insert({0,1});
    mapy.insert({0,2});

    for(auto &[key,value]:mapy)
    {
        cout<<key<<"=>"<<value<<endl;
    }

    this syntax doesn't work in multimap  
    mapy[0]=1;
    mapy[0]=2;
*/

/*
NOTE:
When using for each on map we write it.first wherease when iterating via a loop we do it->first
*/
int main()
{

    return 0;
}