#include <bits/stdc++.h>
using namespace std;

/*
general syntax of stringstream
stringstream ss("string to be converted to stream");
stringstream ss(words);
string dummy;

while(ss>>dummy)
{
    // do something with dummy
}
*/

/*
use of stringstream based on particular parameter
    vector<int> ans;
    stringstream ss(str);

    string dummy;

    while(getline(ss, dummy, ','))
    {
        int pushy= stoi(dummy);
        ans.push_back(pushy);
    }


*/
int main()
{

    return 0;
}