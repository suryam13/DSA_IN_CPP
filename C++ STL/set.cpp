#include<bits/stdc++.h>
using namespace std;

/*
lower_bound and upper_bound are used to find the first element which is greater than or equal to the given value and the first element which is greater than the given value respectively.

syntax:
st.lower_bound(value) 

//genral syntax used in containers with random access iterators
lower_bound(v.begin(),v.end(),value);
*/
 int main()
{
    
    return 0;
}